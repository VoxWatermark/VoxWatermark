# No-Box Perturbation Tutorial

### Step 1: Extract the "noise_wav.zip" file
Download the `noises_wav.zip` file and extract it to a directory of your choice.

### Step 2: Specify the Directory for Noise Files
In line 583 of the script, specify the directory where you extracted the `noise_wav.zip` file. Modify the following line in the code:

### Step 3: Specify the Directory for Watermarked and Unwatermarked datasets
In line 839, specify the directory for both the unwatermarked and watermarked datasets.

Then you can run the code :)

# Black-Box Perturbation Tutorial

In the main function specify the directory for the watermarked dataset according to the method.

Then you can run the code :)

# White-Box Perturbation Tutorial

Note that forgery is for fooling the decoder to behave an unwatermarked audio as a watermarked audio.
Note that removal is for fooling the decoder to bahave a watermarked audio as an unwatermarked audio.

In the main function Specify the directories.
